/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, Java, PHP, Ruby, Perl,
C#, OCaml, VB, Swift, Pascal, Fortran, Haskell, Objective-C, Assembly, HTML, CSS, JS, SQLite, Prolog.
Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
#include <stdio.h>

// Функція для обчислення кількості шуканих послідовностей
int countSequences(int n) {
    int dp[n+1];
    
    // Ініціалізуємо базові значення
    dp[0] = 1;
    dp[1] = 2;
    dp[2] = 4;
    
    // Обчислюємо кількість шуканих послідовностей для кожної довжини
    for (int i = 3; i <= n; i++) {
        // Кожна нова послідовність може закінчуватися нулем або одиницею
        // Якщо закінчується нулем, то перед ним може бути будь-яка допустима послідовність довжиною (i-1)
        // Якщо закінчується одиницею, то перед нею може бути будь-яка допустима послідовність довжиною (i-2)
        dp[i] = (dp[i-1] + dp[i-2]) % 12345;
    }
    
    // Повертаємо кількість шуканих послідовностей довжиною n
    return dp[n];
}

int main() {
    int n, result;

    printf("Введіть довжину послідовності n (1 < n < 10000): ");
    scanf("%d", &n);
    
    // Перевіряємо чи введена довжина є в межах 1 < n < 10000
    if (n <= 1 || n >= 10000) {
        printf("Неправильне введення. Будь ласка, введіть число від 1 до 10000.\n");
        return 1; // Повертаємо ненульове значення, щоб позначити помилку.
    }

    // Знаходимо кількість шуканих послідовностей довжиною n
    result = countSequences(n);

    printf("Кількість шуканих послідовностей: %d\n", result);

    return 0;
}
